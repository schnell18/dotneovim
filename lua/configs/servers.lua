-- list of all lsp servers configured.
-- consumed by configs.lspconfig (to enable them) and
-- configs.mason-lspconfig (to install them).
return {
  "bashls",
  "gopls",
  "lua_ls",
  "pylsp",
  -- "r_language_server",
  "texlab",
  "ts_ls",
}
